version: "3"
services:
  db:
    image: mongo
    restart: always
    ports:
      - "27017:27017"
    volumes:
      - /data/db
    networks:
      - net
  app:
    depends_on:
      - db
    build: .
    ports:
      - "5000:5000"
    restart: always
    networks:
      - net
networks:
  net: